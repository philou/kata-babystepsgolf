import {doSomething} from "../src/BabyStepsGolf";

describe("baby_steps_golf", () => {
    // TODO Replace with an acceptance test relevant for this kata
    test('acceptance test', () => {
        expect(doSomething()).toEqual(42);
    });
});
